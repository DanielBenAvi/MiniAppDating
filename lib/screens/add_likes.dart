import 'package:flutter/material.dart';

class AddLikesScreen extends StatelessWidget {
  const AddLikesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Likes Screen'),
      ),
      body: Center(
        child: const Text('Add Likes Screen'),
      ),
    );
  }
}
