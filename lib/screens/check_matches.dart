import 'package:flutter/material.dart';

class CheckMatchesScreen extends StatelessWidget {
  const CheckMatchesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Check Matches Screen'),
      ),
      body: Center(
        child: const Text('Check Matches Screen'),
      ),
    );
  }
}
