import 'package:flutter/material.dart';

class CheckLikesScreen extends StatelessWidget {
  const CheckLikesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Check Likes Screen'),
      ),
      body: Center(
        child: const Text('Check Likes Screen'),
      ),
    );
  }
}
